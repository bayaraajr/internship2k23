import Layout from '@/components/Layout';
import React from 'react';

const Agenda = (props: any) => {
    return (
        <Layout>
            <p className="text-lime">Agenda</p>
        </Layout>
    )
}

export default Agenda;